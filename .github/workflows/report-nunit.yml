# Deprecated: test reporting now runs in ci.yml (report-tests job) to avoid workflow_run token/403 issues
# and concurrency deadlocks with stuck queued runs.
name: Annotate CI run with test results

on:
  workflow_dispatch:

permissions:
  contents: read
  actions: read
  checks: write

jobs:
  annotate:
    name: Annotate CI run with test results
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: echo "Disabled - use Continuous Integration workflow instead."
